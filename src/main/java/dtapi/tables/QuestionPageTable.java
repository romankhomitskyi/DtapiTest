package dtapi.tables;

import org.apache.logging.log4j.Logger;
import org.openqa.selenium.WebDriver;

public class QuestionPageTable extends BaseTable {
    public QuestionPageTable(WebDriver driver, Logger log) {
        super(driver, log);
    }
}
