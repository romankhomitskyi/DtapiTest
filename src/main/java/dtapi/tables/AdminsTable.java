package dtapi.tables;

import org.apache.logging.log4j.Logger;
import org.openqa.selenium.WebDriver;

public class AdminsTable extends BaseTable {
    public AdminsTable(WebDriver driver, Logger log) {
        super(driver, log);
    }
}
